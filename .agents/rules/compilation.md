# Compilation Rule

Whenever any source code or configuration changes are made in this repository:
1. Always immediately recompile the project (e.g. using `cmake --build build_COMPILE_FRESH` or the appropriate build directory) to ensure binaries (`devilutionx.exe`) reflect the latest changes.
2. Keep the `docs/CHANGELOG.md` updated with notable changes.
